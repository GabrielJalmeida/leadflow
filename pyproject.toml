[project]
name = "leadflow-agent"
version = "0.1.3"
description = "Open-source lead research agent with BYOK providers."
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Move Heaven"}]

[project.scripts]
leadflow = "leadflow_agent.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
